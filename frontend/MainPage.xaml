<Page
    x:Class="frontend.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:frontend"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Padding="20">

            <!-- Input -->
            <TextBox Header="First Name"
             Text="{Binding FirstName, Mode=TwoWay}" />

            <TextBox Header="Last Name"
                     Text="{Binding LastName, Mode=TwoWay}" />

            <!-- Button -->
            <Button Content="Add Farmer"
            Click="AddFarmer_Click"/>

            <!-- List -->
            <ListView ItemsSource="{Binding Farmers}" IsItemClickEnabled="True" ItemClick="ListView_ItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock Text="{Binding FirstName}" />
                            <TextBlock Margin="10 0 0 0" Text="{Binding LastName}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </Grid>
</Page>
