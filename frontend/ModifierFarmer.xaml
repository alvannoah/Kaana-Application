<Page
    x:Class="Frontend.ModifierFarmer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Frontend"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Farmers List -->

        <!-- Edit Section -->
        <StackPanel Grid.Row="1"
                Margin="0,20,0,0"
                Spacing="10">

            <TextBlock Foreground="White" Text="{Binding SelectedFarmer.Name }" FontSize="20" />

            <TextBox Header="First Name"
                 Text="{Binding SelectedFarmer.FirstName, Mode=TwoWay}" />

            <TextBox Header="Last Name"
                 Text="{Binding SelectedFarmer.LastName, Mode=TwoWay}" />

            <Button Content="Update Farmer"
                Click="Button_Click_1" />

            <Button Content="Back"
                    Click="Button_Click"/>
        </StackPanel>

    </Grid>
</Page>
